Post code complete tsting issues
================================


iOS push registration if user doesn't allow notifications ...

scenarions


1) App is installed and first time it is launched user hits don't allow

    registerForPush() called

    in delegate didRegisterForRemoteNotificationsWithDeviceToken with a token AND popup visible

    in didRegisterUserNotificationSettings with notificationSettings.types false

2) subsequent launch of the app (with push still off)

    registerForPush() called

    didRegisterUserNotificationSettings  with notificationSettings.types false

3) User goes into settings and turns  on push 

    registerForPush() called

    didRegisterForRemoteNotificationsWithDeviceToken delegate fires

    didRegisterUserNotificationSettings fires with notificationSettings.types TRUE

4) User goes into settings and turns off push

